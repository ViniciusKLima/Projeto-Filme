<header>
    <h1>LocMovies</h1>
    <p>Seu catálogo de filmes favoritos</p>
</header>

<div class="container-catalogo">
    <h2 class="titulo-secao">Destaques</h2>
    <div class="grid-filmes" id="gridFilmes">
        <?php if (!empty($filmes)): ?>
            <?php foreach ($filmes as $filme): ?>
                <div class="card-filme" onclick="window.location.href='/filme/detalhes?id=<?= $filme->getId() ?>'">
                    <img src="/images/<?= $filme->getCapaPrincipal() ?>" alt="<?= $filme->getNome() ?>" />
                    <div class="info-filme">
                        <h3><?= $filme->getNome() ?></h3>
                        <p><?= $filme->getAnoLancamento() ?></p>
                        <div class="nota-filme">⭐ <?= $filme->getNota() ?></div>
                    </div>
                </div>
            <?php endforeach; ?>
        <?php else: ?>
            <p>Nenhum filme disponível.</p>
        <?php endif; ?>
    </div>
</div>

<footer>© LocMovies. Todos os direitos reservados.</footer>
